import { styled } from "../../styles/stitches.config";

const PostImageMask = styled("div", {
	position: "absolute",
	width: "100%",
	height: "100%",
	background: "hsl(0deg 0% 0% / 30%)",
	zIndex: 2,
});

export default PostImageMask;
